/*
Reputable Product Agency (RPA) has started receiving complaints 
from their credit card processor about fraudulent transactions. 
Help your finance department identify potentially risky transactions 
before they finish processing.
*/

CREATE TABLE transaction_data ( 
    id INTEGER, 
    full_name TEXT, 
    email TEXT, 
    zip INTEGER, 
    ip_address TEXT
);

-- What are the column names?
SELECT *
FROM transaction_data
LIMIT 10;
